import 'package:flutter/material.dart';

class GameRecordPage extends StatelessWidget {
  const GameRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Game Records')));
  }
}
